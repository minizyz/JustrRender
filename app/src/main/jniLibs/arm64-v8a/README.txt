# Place compiled libjustr_render.so here for arm64-v8a
# Build with NDK:
#   cd app/src/main/jni
#   ndk-build NDK_PROJECT_PATH=. APP_BUILD_SCRIPT=./Android.mk NDK_APPLICATION_MK=./Application.mk
# Then copy:
#   cp ../../../libs/arm64-v8a/libjustr_render.so ./
